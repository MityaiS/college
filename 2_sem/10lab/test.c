#include <stdio.h>


int ** in_mat(int **M, const char * in_n)
{
    int n, m, R, i, j;
    printf("Type in n\n");
    scanf("%d", &n);
    printf("Type in m\n");
    scanf("%d", &m);
    printf("Type in R\n");
    scanf("%d", &R);
    M[n][m];
    for (i=0;i<n;i++)
    {
        for (j=0;j<m;j++)
        {
            scanf("%d", &M[n][m]);
        }
    }
}

int * decide(int **A, int ** B, int R, int n, int m)
{
    for (int i = 0; i < n; i++)
    {
        
    }
}

int main()
{
    const char * in_n = "in.txt";
    int n, m, R;
    int **A;
    int **B;


    return 0;
}